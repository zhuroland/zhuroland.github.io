---
import Layout from "../../../layouts/SokobanLayout.astro";
---

<Layout title="Light Tiles Off" description="Turn off all the lights to win!">
  <div class="game-wrapper">
    <div class="nav-links">
      <a href="/games" class="btn-link">Back to Games</a>
      <a href="/games/light-tiles-off/maker" class="btn-link">Level Maker</a>
    </div>
    <h1>Light tiles off (or on)</h1>
    
    <div class="controls-top">
      <h2 data-level-display>Level: 1/--</h2>
      <button data-reset-btn>Reset level</button>
    </div>

    <div class="controls-mid">
      <button data-prev-btn>← Previous lvl</button>
      <span>Tries: <span data-tries-display>0</span></span>
      <button data-next-btn>Next lvl →</button>
    </div>

    <canvas data-game-canvas width="270" height="270"></canvas>

    <div data-win-message class="message hidden">
      <span>You solved it</span>
      <button data-next-level-btn>Go to next level</button>
    </div>

    <div data-end-message class="message hidden">
      <span>You've done all the puzzles, congratulations!</span>
      <button data-restart-btn>Restart</button>
    </div>
    
    <footer>
        By <a href="https://github.com/TheMysteryz" target="_blank">TheMysteryz</a>
    </footer>
  </div>
</Layout>

<style is:global>
  *, *::before, *::after {
    box-sizing: border-box;
  }
  body {
    margin: 0;
    padding: 0;
    overflow-x: hidden;
    overflow-y: auto;
    background-color: oklch(98% 0.005 250);
  }
</style>  

<style>
  .game-wrapper {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-start;
    min-height: 100dvh;
    width: 100%;
    max-width: 720px;
    margin: 0 auto;
    padding: clamp(0.75rem, 2vw, 1.5rem);
    gap: 1rem;
    background-color: oklch(98% 0.005 250); /* slate-50 */
    font-family: 'Inter', system-ui, -apple-system, sans-serif;
    color: oklch(32% 0.02 250); /* slate-700 */
  }
  .nav-links {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1rem;
    width: 100%;
  }
  .btn-link {
    padding: 0.45rem 0.85rem;
    background-color: oklch(100% 0 0 / 0.5);
    border: 1px solid oklch(86% 0.01 250 / 0.5);
    border-radius: 9999px;
    cursor: pointer;
    font-weight: 500;
    text-decoration: none;
    color: oklch(53% 0.02 250);
    font-size: 0.875rem;
    transition: all 0.2s;
  }
  .btn-link:hover { 
    background-color: oklch(100% 0 0 / 0.8); 
    color: oklch(15% 0.02 250);
    border-color: oklch(69% 0.02 250 / 0.7);
    box-shadow: 0 1px 2px rgba(0,0,0,0.05);
  }

  h1 { 
    font-size: clamp(1.65rem, 4vw, 2rem); 
    line-height: 1.2; 
    margin: 0.25rem 0 0.25rem; 
    font-weight: 700;
    color: oklch(15% 0.02 250);
    letter-spacing: -0.025em;
  }
  
  .controls-top, .controls-mid {
    display: flex;
    justify-content: space-between;
    width: 100%;
    max-width: 100%;
    align-items: center;
    gap: 0.5rem;
  }
  
  button {
    padding: 0.45rem 0.8rem;
    background-color: oklch(100% 0 0 / 0.7);
    border: 1px solid oklch(92% 0.01 250 / 0.6);
    border-radius: 0.5rem;
    cursor: pointer;
    font-weight: 600;
    color: oklch(42% 0.02 250);
    transition: all 0.2s;
    box-shadow: 0 1px 2px rgba(0,0,0,0.05);
  }
  button:hover { 
    background-color: oklch(98% 0.005 250 / 0.9); 
    color: oklch(15% 0.02 250);
    border-color: oklch(86% 0.01 250 / 0.8);
    box-shadow: 0 2px 5px -2px oklch(15% 0.02 250 / 0.3);
  }
  button:disabled { 
    opacity: 0.5; 
    pointer-events: none; 
    box-shadow: none;
  }
  
  button[data-reset-btn] {
    background-color: oklch(62% 0.18 25 / 0.08);
    color: oklch(62% 0.18 25 / 0.8);
    border-color: oklch(62% 0.18 25 / 0.15);
  }
  button[data-reset-btn]:hover {
    background-color: oklch(62% 0.18 25 / 0.2);
    color: oklch(62% 0.18 25);
    border-color: oklch(62% 0.18 25);
    box-shadow: 0 4px 10px -2px oklch(62% 0.18 25 / 0.3);
  }
  
  canvas {
    width: min(100%, 100vw);
    height: auto;
    background-color: oklch(100% 0 0 / 0.6);
    cursor: pointer;
    margin-bottom: 0.5rem;
    border-radius: 1rem;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
    padding: 8px;
    border: 1px solid oklch(100% 0 0 / 0.3);
  }

  .message {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    font-weight: 600;
    background: oklch(100% 0 0 / 0.85);
    padding: 1.5rem;
    border-radius: 1rem;
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    z-index: 10;
    min-width: 280px;
    backdrop-filter: blur(16px);
    border: 1px solid oklch(100% 0 0 / 0.4);
  }
  .hidden { display: none; }
  
  footer {
    position: static;
    width: 100%;
    padding: 0.75rem 0 0;
    font-size: 0.875rem;
    text-align: center;
    color: oklch(69% 0.02 250);
  }
  a { color: oklch(53% 0.02 250); font-weight: 600; text-decoration: none; transition: color 0.2s; }
  a:hover { color: oklch(15% 0.02 250); }

  @media (max-width: 420px) {
    .game-wrapper {
      padding: 0.5rem;
      gap: 0.85rem;
    }

    .nav-links {
      gap: 0.5rem;
    }

    .btn-link {
      padding: 0.4rem 0.7rem;
      font-size: 0.8rem;
    }

    .controls-top, .controls-mid {
      gap: 0.35rem;
    }

    button {
      padding: 0.4rem 0.7rem;
      font-size: 0.82rem;
    }

    canvas {
      padding: 6px;
    }
  }
</style>

<script>
  import { levels } from "./levels";

  let currentLevel = 0;
  let tiles: number[][] = [];
  let tries = 0;
  let isWin = false;
  let isEnd = false;

  // Canvas config
  const TILE_SIZE = 50;
  const GAP = 5;
  // 5 tiles * 50px + 4 gaps * 5px = 250 + 20 = 270px
  const CANVAS_SIZE = (TILE_SIZE * 5) + (GAP * 4); 

  let canvas: HTMLCanvasElement | null;
  let ctx: CanvasRenderingContext2D | null;
  let levelDisplay: HTMLElement | null;
  let triesDisplay: HTMLElement | null;
  let prevBtn: HTMLButtonElement | null;
  let nextBtn: HTMLButtonElement | null;
  let resetBtn: HTMLElement | null;
  let winMessage: HTMLElement | null;
  let nextLevelBtn: HTMLElement | null;
  let endMessage: HTMLElement | null;
  let restartBtn: HTMLElement | null;

  function initGame() {
    canvas = document.querySelector('[data-game-canvas]') as HTMLCanvasElement;
    levelDisplay = document.querySelector('[data-level-display]');
    triesDisplay = document.querySelector('[data-tries-display]');
    prevBtn = document.querySelector('[data-prev-btn]') as HTMLButtonElement;
    nextBtn = document.querySelector('[data-next-btn]') as HTMLButtonElement;
    resetBtn = document.querySelector('[data-reset-btn]');
    winMessage = document.querySelector('[data-win-message]');
    nextLevelBtn = document.querySelector('[data-next-level-btn]');
    endMessage = document.querySelector('[data-end-message]');
    restartBtn = document.querySelector('[data-restart-btn]');

    if (!canvas) return;
    resizeCanvas();
    ctx = canvas.getContext('2d');

    loadLevel(currentLevel);
    setupListeners();
  }

  function resizeCanvas() {
    if (!canvas) return;

    const isMobile = window.innerWidth <= 420;
    const horizontalPadding = isMobile ? 16 : 32;
    const reservedHeight = isMobile ? 170 : 260;
    const maxBoardSize = isMobile ? window.innerWidth - horizontalPadding : 360;
    const availableWidth = Math.max(window.innerWidth - horizontalPadding, 240);
    const availableHeight = Math.max(window.innerHeight - reservedHeight, 240);
    const targetSize = Math.max(240, Math.min(availableWidth, availableHeight, maxBoardSize));
    const tileSize = Math.floor((targetSize - (GAP * 4)) / 5);
    const boardSize = (tileSize * 5) + (GAP * 4);

    canvas.width = boardSize;
    canvas.height = boardSize;
    canvas.style.width = "100%";
    canvas.style.maxWidth = `${boardSize}px`;
    canvas.style.height = "auto";
  }

  function loadLevel(lvlIndex: number) {
    currentLevel = lvlIndex;
    // Deep copy level
    tiles = levels[currentLevel].map(row => [...row]);
    tries = 0;
    isWin = false;
    isEnd = false;
    render();
  }

  function render() {
    if (!ctx || !levelDisplay || !triesDisplay || !prevBtn || !nextBtn || !winMessage || !endMessage) return;

    // Update UI
    levelDisplay.textContent = `Level: ${currentLevel + 1}/${levels.length}`;
    triesDisplay.textContent = tries.toString();
    
    prevBtn.disabled = currentLevel === 0;
    nextBtn.disabled = currentLevel === levels.length - 1;
    
    if (currentLevel === 0) prevBtn.style.opacity = '0';
    else prevBtn.style.opacity = '1';
    
    if (currentLevel === levels.length - 1) nextBtn.style.opacity = '0';
    else nextBtn.style.opacity = '1';

    // Render Canvas
    // Clear with white background for the "card" effect inside canvas if needed, 
    // but we are drawing full tiles.
    ctx.clearRect(0, 0, canvas!.width, canvas!.height);
    const tileSize = Math.floor((canvas!.width - (GAP * 4)) / 5);
    const boardSize = (tileSize * 5) + (GAP * 4);
    const offsetX = Math.max((canvas!.width - boardSize) / 2, 0);
    const offsetY = Math.max((canvas!.height - boardSize) / 2, 0);
    
    tiles.forEach((row, rIndex) => {
      row.forEach((val, cIndex) => {
        const x = offsetX + cIndex * (tileSize + GAP);
        const y = offsetY + rIndex * (tileSize + GAP);
        
        // Draw tile
        // Modern colors: 
        // On (1) = Slate 800 (Dark) or maybe a nice accent color? Let's go with a soft dark blue/slate.
        // Off (0) = Slate 100 (Light)
        
        // Rounded rect function
        const radius = 8;
        ctx!.beginPath();
        ctx!.roundRect(x, y, tileSize, tileSize, radius);
        
        if (val === 1) {
            // ON state - Dark/Active
            ctx!.fillStyle = 'oklch(32% 0.02 250)'; // slate-700
            ctx!.fill();
            
            // Add a subtle inner shadow or highlight?
            // Let's keep it flat and clean for now.
        } else {
            // OFF state - Light/Inactive
            ctx!.fillStyle = 'oklch(96% 0.005 250)'; // slate-100
            ctx!.fill();
        }
      });
    });

    // Win/End states
    winMessage.classList.toggle('hidden', !isWin);
    endMessage.classList.toggle('hidden', !isEnd);
  }

  function handleCanvasClick(e: MouseEvent) {
    if (isWin || isEnd || !canvas) return;

    const rect = canvas.getBoundingClientRect();
    const x = e.clientX - rect.left;
    const y = e.clientY - rect.top;

    const tileSize = Math.floor((canvas.width - (GAP * 4)) / 5);
    const boardSize = (tileSize * 5) + (GAP * 4);
    const scaleX = canvas.width / rect.width;
    const scaleY = canvas.height / rect.height;
    const boardX = x * scaleX;
    const boardY = y * scaleY;
    const offsetX = Math.max((canvas.width - boardSize) / 2, 0);
    const offsetY = Math.max((canvas.height - boardSize) / 2, 0);

    const col = Math.floor((boardX - offsetX) / (tileSize + GAP));
    const row = Math.floor((boardY - offsetY) / (tileSize + GAP));

    // Check bounds
    if (row >= 0 && row < 5 && col >= 0 && col < 5) {
        // Check if click was in the gap
      const tileX = offsetX + col * (tileSize + GAP);
      const tileY = offsetY + row * (tileSize + GAP);
      if (boardX >= tileX && boardX <= tileX + tileSize && boardY >= tileY && boardY <= tileY + tileSize) {
            handleClick(row, col);
        }
    }
  }

  function handleClick(r: number, c: number) {
    toggle(r, c);
    if (r > 0) toggle(r - 1, c);
    if (r < tiles.length - 1) toggle(r + 1, c);
    if (c > 0) toggle(r, c - 1);
    if (c < tiles[0].length - 1) toggle(r, c + 1);

    tries++;
    checkWin();
    render();
  }

  function toggle(r: number, c: number) {
    tiles[r][c] = tiles[r][c] ? 0 : 1;
  }

  function checkWin() {
    const allOff = tiles.every(row => row.every(val => val === 0));
    if (allOff) {
      if (currentLevel + 1 >= levels.length) {
        isEnd = true;
      } else {
        isWin = true;
      }
    }
  }

  function setupListeners() {
    if (resetBtn) resetBtn.onclick = () => loadLevel(currentLevel);
    
    if (prevBtn) prevBtn.onclick = () => {
      if (currentLevel > 0) loadLevel(currentLevel - 1);
    };
    
    if (nextBtn) nextBtn.onclick = () => {
      if (currentLevel < levels.length - 1) loadLevel(currentLevel + 1);
    };

    if (nextLevelBtn) nextLevelBtn.onclick = () => {
      if (currentLevel < levels.length - 1) loadLevel(currentLevel + 1);
    };

    if (restartBtn) restartBtn.onclick = () => {
      loadLevel(0);
    };

    if (canvas) {
        canvas.onclick = handleCanvasClick;
    }
  }

  initGame();
  document.addEventListener('astro:page-load', initGame);
  window.addEventListener('resize', () => {
    resizeCanvas();
    render();
  });
</script>
