---
layout: '../layouts/DocsLayout.astro'
title: 'About me'
author: 'Roland ZHU'
---

import conf from "../config.json";
import { calculateAge } from "../utils";
import GoBackLink from "../components/GoBackLink.astro"
import MediaLinks from "../components/MediaLinks.astro"
import ContactMe from "../components/ContactMe.astro"

export const socialLinks = Object.entries(conf.infos.socials).map(([key, value]) => (
	<li>
		<MediaLinks media={key} value={value} />
	</li>
))

<GoBackLink />

## Me

Hello I'm {conf.infos.author}, I'm {calculateAge(conf.infos.birthdate)} years old, I'm a full-stack developper.

## Socials

<ul>
	{socialLinks}
</ul>

### [Contact me](/contactme)
