
<link rel="stylesheet" href="https://chatbotui.teamzbl.com/zbl-chatbot-ui.css" />

<slot/>

<script type="module" is:inline src="https://chatbotui.teamzbl.com/zbl-chatbot-ui.iife.js"></script>
<script>
    // Type declaration for ZBLChatbotUI
    declare global {
      interface Window {
        ZBLChatbotUI?: {
          init: (token: string, config: {
            serverUrl: string;
            projectId: string;
            chatbotId: string;
          }) => void;
        };
      }
    }

    document.addEventListener("DOMContentLoaded", function () {
      if (window.ZBLChatbotUI) {
        window.ZBLChatbotUI.init("eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.e30.8VKCTiBegJPuPIZlp0wbV0Sbdn5BS6TE5DCx6oYNc5o", {
          serverUrl: "https://chatbot-server-f0jd.onrender.com",
          projectId: "roland-portfolio",
          chatbotId: "96311b55-2dd7-46f5-bbe0-a93665b30fad"
        });
      } else {
        console.error("ZBLChatbotUI is not defined");
      }
    });
  </script>
